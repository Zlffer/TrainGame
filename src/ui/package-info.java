
package ui;

/*
 * Handles the presentation layer and all direct user interaction. This package
 * is responsible for rendering the console menus (MainMenu, GameMenu),
 * capturing user input, and managing the application's navigational state.
 * It delegates user requests to the appropriate command in the 'actions' package.
 */
