
package logic;

/*
 * Contains all core business logic for the application, acting as the
 * service layer. This package is responsible for calculations (e.g., passenger
 * counts), state management (e.g., moving the train), and executing
 * business rules (e.g., sorting carriages). It is decoupled from the UI
 * and is invoked by the 'actions' package.
 */
