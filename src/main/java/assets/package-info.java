
package assets;

/*
 * Contains all domain objects (data models) and static definitions for the
 * application. This package includes the core entities (Train, Locomotive,
 * Carriage) and their class hierarchies. It also holds enumerations (enum)
 * that define fixed data sets, such as TicketType and the Station route list.
 */
